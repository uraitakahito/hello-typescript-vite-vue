# ESLint 10 系と eslint-plugin-import (peerOptional 経由で引き込まれる) の
# peer 範囲が衝突するため、npm の厳格な peer 解決を緩めて既存の組合せを
# そのまま通す。ref: uraitakahito/hello-typescript-vite と同じ依存組成を
# 維持しつつ、npm 7+ の自動 peer install を抑制する。
legacy-peer-deps=true
